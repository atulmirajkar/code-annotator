package fixture

const layoutMessage = "This base-side line is deliberately long enough to require horizontal scrolling without crossing into the current pane or obscuring its change highlight."

var afterSelection = true
