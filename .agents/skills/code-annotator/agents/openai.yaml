interface:
  display_name: "Code Annotator"
  short_description: "Process document review annotations safely"
  default_prompt: "Process actionable code-annotator annotations using the live API when a server runs, or offline commands only when it is stopped."
